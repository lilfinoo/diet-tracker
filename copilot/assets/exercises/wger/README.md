# Exercise media from wger

These files were downloaded from the public wger API and are served locally by Diet Tracker. Author, source, license, AI-generation status, and exercise associations are recorded in `manifest.json`.

Run `python scripts/import_wger_media.py` from the repository root to refresh the files and generated metadata. Do not add files to this directory without updating the manifest and preserving their attribution and license terms.
